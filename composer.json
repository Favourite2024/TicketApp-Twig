{
    "name": "favour/ticketapp-twig",
    "description": "Ticket App (Twig version)",
    "type": "project",
    "require": {
      "twig/twig": "^3.0"
    },
    "autoload": {
      "psr-4": {
        "App\\": "src/"
      }
    }, "scripts": {
      "post-install-cmd": [
      "mkdir -p cache",
      "chmod 755 cache"
    ]
}
  }
  